---
name: team-status
description: Show the persistent roles team dashboard
---

Run the team status script. Execute: `bash "${CLAUDE_PLUGIN_ROOT}/scripts/team-status.sh"`

If the user passes `--history` or `--alerts`, pass that flag to the script.
